!!!
%html(lang="en-US")
  %head
    %title Pinboard
    = stylesheet_link_tag "application"
    = javascript_include_tag "application"
    = csrf_meta_tags

  %body
    - if flash.any?
      - flash.each do |flash_type, message|
        .flash{ class: flash_type }= message
    = yield
